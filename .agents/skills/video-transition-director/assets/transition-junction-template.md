# 转场接点设计

> 只填写当前任务需要的字段。小范围接点可直接使用同样结构写进现有分镜，不必另建文件。

## 接点 `<JUNCTION-ID>`：`<A镜> → <B镜>`

- **叙事关系：** `<动作延续 / 信息揭示 / 时间跳跃 / 空间切换 / 情绪联想 / 因果对照 / 刻意冲击>`
- **观众注意力：** `<切前看/听什么 → 切后落到什么>`
- **必须保持：** `<身份、造型、人数、道具、空间、方向、声音等>`
- **必须改变：** `<时间、地点、状态、风格、材质等>`
- **主铰链：** `<动作 / 视线 / 声音 / 遮挡 / 图形 / 运镜 / 光影 / 生成式变化>`
- **辅助铰链：** `<没有则写“无”>`
- **执行路线：** `<EDIT_ONLY / AI_ASSISTED_EDIT / GENERATED_BRIDGE / IN_SHOT_GENERATIVE_TRANSITION / ENDPOINT_CONDITIONED>`
- **选择理由：** `<为什么这是最小有效方案>`

### A 镜出口

`<主体与位置；动作相位和方向；摄影机运动；焦点/亮度/色彩；声音如何进入接点>`

### 铰链

`<切点或变化发生的可见机制；谁/什么承担覆盖或匹配；观众此刻看/听什么>`

### B 镜入口

`<如何继承动作、方向、构图、焦点或声音；第一个完整可读状态；何时落稳>`

### 剪辑与声音

- **视频切点/曲线：** `<硬切、叠化、遮挡最深处、运动模糊峰值等>`
- **声音桥：** `<J/L、环境底、动作声、对白、音乐拍点；来源与交叠关系>`
- **后期处理：** `<裁切、调色、重定时、光流、合成层；没有则写“无”>`

### 生成与引用

- **模型可见内容：** `<只写需要生成的画面机制>`
- **referencePlan：** `<稳定资产ID/相对路径/职责/状态；不虚构 Node>`
- **关键帧状态：** `<不使用 / 候选待用户审核 / 已按具体版本和用途通过>`
- **能力待核验：** `<模型、端点控制、时长、画幅、声音等>`

### 风险与 QA

- **最大风险：** `<只列会破坏剧情或接镜的核心风险>`
- **重点窗口：** `<A镜动作阶段 → 铰链 → B镜完整状态>`
- **HARD_REJECT：** `<错人、错物、错空间、形变、声音等当前具体条件>`
- **可接受备注：** `<不影响当前用途的轻微偏差>`
- **状态：** `<DESIGNED / READY_FOR_EDIT / READY_FOR_AUTHORIZED_TRIAL / PASS_WITH_NOTES / HARD_REJECT / HUMAN_ACCEPTANCE_PENDING>`
