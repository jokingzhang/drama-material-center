# {{status}}｜{{taskId}}｜{{title}} {{version}}

〖风格〗<画面类型>，{{durationSeconds}} 秒，{{aspectRatio}}，<主要场景质感、色光和整体速度；只写当前任务真实需要且已核对的生成或后期边界，不为凑字数扩写>。

〖空间与轴线〗

- <写清入口、出口、危险侧、安全侧、人物、摄影机和关键道具的相对位置；复杂动作可按时间段分行，简单任务也至少用一句话锁定位置和方向>

〖时间轴〗

- `<起始–结束时间>`：<观众从哪里看；人物先做什么，碰到谁或什么，直接造成什么；这段发生的对白必须逐字写在这里，并带着说话人的动作、口型和现场反应>
- `<下一起始–结束时间>`：<继续主要动作因果，直到最后一秒的人物、道具和危险状态稳定；全部区间连续覆盖 0–{{durationSeconds}}s，不重叠、不留空档>

〖声音〗<逐句重复时间轴中的说话人、逐字对白正文、时间窗口和声音职责，再汇总环境声、动作声、声音桥及是否有配乐、旁白和字幕；不得用“见时间轴”代替逐字正文，也不得在这里新增对白或改变时序>。

〖参考〗<只列经过当前最小参考与像素相容闸门的 referencePlan.assets，按实际输入顺序逐项关联：场景、当前造型人物标准图、状态/道具/声音、必要局部辅助。每项先写对应主体，再紧邻其引用，随后写唯一职责。人物标准图只锁身份和同一当前造型的体型、轮廓及前侧背细节；不默认再加同一人物头像或第二张三视图。标准图引用后的职责说明须写明不得复制多视排版、中性站姿、重复人物、拼板、文字或影棚背景，但这些文字不能消除冲突像素。必要局部辅助使用“只锁”“只参考”或“仅约束”明确其具体职责。关系帧仅在静态关系是主要风险时使用，首尾控制须模型明确支持；不引用 INTERNAL、REJECTED、SUPERSEDED 或计划外输入>。

〖禁止〗<可选；只有当前任务存在高概率、会直接破坏主要画面且无法用正向时间轴充分避免的错误时才保留本段，否则删除整段>。
