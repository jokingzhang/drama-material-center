# 短剧制作 SOP

适用于本仓库内的剧本、对白、素材、分镜、试片、视频生产、返修及交付。分镜的目标是让观众连贯地理解人物、动作、空间、情绪和声音；镜头设计、生成划分和剪辑共同服务这个目标。在生成前排除已知错误，用小范围试片验证真正未知的模型表现，并在交付前完成画面、声音和接镜检查。

本文件是仓库共用操作规范，不存放任何短剧的实际生产资料。素材目录、业务字段和状态沿用 [AGENTS.md](AGENTS.md)；项目事实沿用实际工作区中的 `PRODUCTION_RULES.md`、正式索引与已检查文件。当前用户明确决定优先于通用方法和历史方案。

## 1. 执行方式与 Skill 选择

由同一个主会话承担编剧、美术、导演、检查和执行职责。使用 [ai-director](.agents/skills/ai-director/SKILL.md) 作为总入口，按问题读取方法；不创建角色团队、Reviewer 或逐镜评分流程。默认主会话创作，只有用户明确指定豆包作者时才使用 `doubao-creative-studio`，并保持该范围的作者选择。

| 当前问题 | 使用方法 | 不额外增加的工作 |
| --- | --- | --- |
| 故事核心、人物动机、结构或场景推进不成立 | 按需选 `sw-premise-theme`、`sw-character-conflict`、`sw-story-structure`、`sw-scene-craft` | 不重写已经确定且未受影响的剧情 |
| 创建、修改或诊断对白 | [sw-dialogue](.agents/skills/sw-dialogue/SKILL.md) | 不为了口语化强加口头禅、职业术语或新事实 |
| 整场信息、节奏、切点和生成单元 | [叙事短片导演分镜](.agents/skills/jimeng-narrative-director/SKILL.md) | 不默认每句话拆一个视频节点 |
| 连续动作、跟随或空间揭示 | [电影级长镜头](.agents/skills/jimeng-cinematic-long-take/SKILL.md) | 不强制加第二个镜头，不把两段未验证的拼接叫作一镜到底 |
| 脸红、克制、停顿、忍笑等局部表演 | [AI演员微表情导演](.agents/skills/jimeng-micro-expression-director/SKILL.md) | 已有明确表演方向时，不再要求用户选一轮试戏方案 |
| 相邻镜头或场景接得生硬，需设计动作、视线、声音、遮挡、图形或生成式转场 | [AI 视频转场导演](.agents/skills/video-transition-director/SKILL.md) | 先修两端状态和剪辑点，不用特效掩盖错人、错空间或错道具 |
| 路线、遮挡、接触、道具状态难以表达 | [Blender 白模](.agents/skills/blender/SKILL.md) | 只做必要局部，不给普通对白配白模，不做四肢精修 |
| 生图或编辑图 | 按 [生图工具默认路由](#image-generation-routing) 选择 `imagegen` 或 `libtv-cli` → Lib Image | 编辑前看原图；沿用当前输入方式与授权范围，不额外启动工具对比 |
| LibTV 能力查询、节点、引用、运行、下载 | 已安装的 `libtv-cli` Skill 及当前 CLI 帮助 | 工具执行不代替故事和导演判断 |

改编、格式、各国编剧方法、风格和案例类 Skill 只在对应问题出现时使用。知识库从 [三个问题域入口](director-knowledge-base/README.md) 按需查阅，不默认读全库。

用户只需说明剧情、想达到的感受和新的想法，不必选 Skill。职责分开：SOP 管执行顺序和边界，`ai-director` 管讨论与方法调度，专项 Skill 解决特定创作问题，知识库保存可按剧情调用的技巧，蒸馏笔记保留来源证据。新教程先融入已有问题域，不默认新增一个 Skill 或复制一套制作流程。

**时长服务剧情与剪辑。** 先按正常表演与接点确定需要多少时间，再用实际入口的时长和参考能力安排生成。模板中的 4–15 秒、默认15秒、镜头数量、画幅、风格和候选数量都不是当前任务硬限制；模型上限只是执行边界，不是每段必须填满的目标。不能把某次30秒或720P配置推广成所有任务标准。

## 2. 从当前任务进入，不重跑整个流程

| 任务 | 起点与结束范围 |
| --- | --- |
| 查询进展、复盘或提建议 | 读取事实和必要证据，保持只读；不触发生产 |
| 新写或修改剧本、对白 | 从第3节开始，更新受影响的下游；仅授权文本时完成本地交付即止 |
| 当前剧本已确定，写分镜 | 先核对台词也已确认，再执行第4—6节；不重写剧情或台词 |
| 已检查稿的上传、绑定、同步或恢复生产 | 验证已有本地成果和引用可复用，进入第6—8节；不因换 Node 或版本号重做创作 |
| 局部返修 | 找到最早出错的层，只修该层和实际受影响的消费者、接镜 |

恢复用户的范围、模型/画质选择、预算、暂停要求、已通过版本和明确审核点。已有授权无需再次询问；“只建节点”“提交后暂停”“先本地审稿”必须停在对应位置。文本授权不自动扩展为外部写入、付费生成或重试。

记录沿用当前剧本、分镜执行表、引用计划和生产记录。只记当前版本、具体问题、处理及结果，不为每一步新增合同、报告或审批表。缺少行政字段不构成重新索取授权的理由。

<a id="current-production-state"></a>
**当前状态只维护一套可执行说明。** 正式索引决定当前文件；现有执行表集中列当前顺序、选片、接点和真实待办，引用计划维护输入映射，运行过程留在生产记录。新版应改正受影响的标题、表格、链接、镜数、时长和状态，不用“本条优先”叠加互相矛盾的旧段落；旧版本保留追溯。可从现有结构化数据取得的值优先派生或链接，不在多份文档里手填副本。文本检查、生成结果、接镜检查与用户接受分别记录。合流前重新读取正式索引，只由一个写入方更新同一正式文件，避免用任务开始时的整份快照盖掉其它范围的新结果。

## 3. 锁定故事、对白和当前事实

进入分镜前首先执行 [剧级画幅硬门禁](AGENTS.md#剧级画幅硬门禁)。每个项目必须锁定横屏或竖屏，后续各集及返工继承；日常生成指令中的相反比例不能覆盖剧级标准。视频节点创建、运行、下载、剪辑与导出分别检查，使用 `scripts/video-format-gate.py`，无锁即阻塞；不替尚未明确的其它项目猜画幅。

**顺序固定：确认剧本 → 确认台词 → 设计分镜。** 确认来自用户对具体版本/范围的决定；可复用已有消息，不因切阶段或恢复任务重复确认。主会话写完、自检通过、登记为当前稿都不能代替这两项确认。普通画面返修不重新打开已确认台词；先读取确认稿再做镜头工作。

1. 检查 Git 状态、实际工作区、目标项目配置、规则、目录和两份正式索引。以当前绑定定位文件，不靠文件名最大版本号、旧交付包或历史聊天猜当前状态。
2. 恢复本场目的、事件顺序、角色当前造型和道具状态。确认哪些对白和剧情已经由用户确定，哪些仍可创作。
3. 对每组交流问：角色此刻要什么、为什么开口、上一句怎样引出下一句、听者怎样反应。职业特点必须服务眼前处境；不能为了一个笑点补出未经支持的职业行为或道具用途。
4. 用户确认对白后，按根规则维护逐字台词合同，锁定正文、说话人和剧情顺序，并保留真实确认出处。未获明确改词指令，发现问题只提出具体建议，不先写改词版下游，更不能先生成。获准修改时先更新剧本、合同，确认修改后的台词再更新分镜；用户直接指定采用的替换句无需再次确认。操作描述使用角色规范全名，自然对白不机械替换代词。
5. 按正常表演估计台词、呼吸、换人、动作和反应共同所需时间。未演读标为估计；装不下先调整节奏和单元划分，不靠加速、删词或截断尾音解决。

**进入分镜前应已明确：** 剧本与台词已有用户确认；事件顺序成立；对白接得上；谁行动、谁反应无歧义；新增事实没有悄悄改变已确认方向。确认后的台词只能按明确改词授权更新，不能以导演修复、口语化或时长不足为由擅改，也不能只改视频节点。

<a id="continuity-first"></a>
## 4. 先设计连贯的成片，再安排镜头与生成单元

新分镜或重大重设计，先读当前剧情；关键方向未定时，按 [分镜讨论方式](.agents/skills/ai-director/references/storyboard-discussion.md) 与用户一起设计：一次只问一个会改变观众理解、人物关系或情绪落点的问题，给出推荐及取舍，等待反馈再推进依赖它的选择。先讨论“观众跟谁看、何时知道、最后感到什么”，不让用户填镜头参数表。已有明确方向、用户要求直接写、局部修补或执行恢复时，不强制增加问答。

随后按 [剧情驱动的技巧选择](director-knowledge-base/分镜提示词/剧情驱动的技巧选择.md) 将目的转成可见／可听机制，再落实到镜头与正文。开场、表演、构图、景深、光线、运镜、声音和剪辑都可参与，但不要求每场用齐；删掉某个技巧不影响本场表达时，不为展示“学会了”而保留。

用户补充信息时，先说明改变了哪项设计，只更新受影响的剧情、分镜、正文和接镜；本场选择留在项目，明确“以后都这样／调整 SOP 或 Skill”的长期要求才更新相应通用规则。沿用现有文档记录决定，不新增讨论表、评分或复审关卡。方向足以支持本次交付后，继续已授权的写作，不在每个阶段重复确认。

一个摄影镜头是一段连续拍摄，一个生成单元是一次模型任务；一个单元可以包含一个或多个真实镜头。先组织完整剧情阶段，再决定观众需要哪些视点及其接法，最后安排生成单元。镜头数和节点数都不是优化目标；不按一句台词一个节点，也不为了减少节点强塞超长动作。用户要求连续视频或合并节点，不自动等于一镜到底。

<a id="generation-unit-design"></a>
**先分配叙事重点与表演时间，再决定任务边界。** 在现有分镜中明确本场观众先知道什么、关系如何变化、最后停在哪里。区分承载剧情的必要动作与日常过程：递接归属、碰落原因、跨越结果等影响理解的变化必须成立；不承担剧情且用户未要求展示的走路、整理等，可设计明确时间省略，并在下一镜建立完成态。不能在宣称连续的动作中跳过必要转换，也不能为减负删除已确认剧情。

- 普通交流保留完整问答与听者反应；每个生成边界都应有实际容量、输入兼容性、叙事阶段或已观察到的模型问题作为依据。
- 接触、道具转换、多人发声与特殊运镜同时争夺注意力时，先调整机位、动作先后和非必要过程，再判断是否分离复杂动作。逐项写全动作不证明它们能在当前构图和时长内完成；顺序动作不能按并行估时。
- 多人发声先验证角色与音色归属。出现错配时，可使用保留原台词与已选音色的说话主镜，让同一声音跨接听者反应画面，或隔离确有问题的发声段；不把一次失败推广成“一句一个节点”或“所有任务只能单音色”。声画分开组织须标明声音来源与覆盖画面，不让反应人物代说画外台词。
- 用户要求的特殊运镜或转场，设计时就区分模型生成与后期承担的部分，写清可观察的成功标志及可用替代路线。例如推轨变焦要同时看主体尺度与背景相对尺度，普通推拉不算实现。用户指定效果必须保留；若还限定实现方式，不能用后期模拟冒充原方式。

这些取舍写入已有备注，无需新建风险表。每次新增动作或拆合单元都重算受影响场次的生成长度与剪后净时长，说明相对目标的变化，不能只延长局部而继续沿用旧总时长。

关键接点在现有分镜备注中说明“上一镜看见什么 → 在哪里切 → 下一镜接住什么”，按实际需要采用：

- **改变景别或角度。** 用有动机的视点变化承接说听、反应或信息，避免把两个近似机位、近似景别但姿势和曝光不一致的独立生成片段硬接成假长镜头。不要求每句切镜，也不以换机位掩盖门位、光源或道具错误。
- **在动作中接。** 确需跨镜延续动作时，前后素材保留同一动作的短重叠覆盖，下一镜从相容的动作阶段继续；剪辑按动作相位、方向、速度趋势和物体归属选择入出点，成片只保留一次动作。重叠长度按动作需要，不规定秒数；生成素材长度与剪后净时长分别核算。跨门、递物等动作的接触到完成必须可追踪，不能靠切镜省掉必要转换。
- **按剧情阶段组接。** 同一完整交流或动作可在一个单元内用多个摄影镜头表达；场景建立、关系变化、信息揭示等阶段也可有动机地跨单元连接，用视线、反应和声音接住。只有实际容量、输入条件、叙事段落或有依据的复杂任务隔离需要时才另开任务，不为保留旧节点编号、素材分组或模板秒数拆段。

切镜回答“观众为什么此刻换一个视点”，拆生成回答“为什么需要另一次任务”。普通方案在同一轮设计中解决，不增加审批、逐镜证明或独立接镜表。已有可用媒体按实际接点复用；只因时长或镜数不同不重做。具体示例与适用边界见[连续性组接方法](director-knowledge-base/分镜提示词/导演设计方法.md#continuity-editing)。

对存在空间、接触、道具变化或接镜风险的单元，在现有分镜中写清：

| 必须想清楚的事实 | 检查方法 |
| --- | --- |
| 起始与完成状态 | 谁在哪里、穿什么、拿什么；结束时谁及道具到哪里 |
| 空间与机位 | 先定房间地标、摄影机物理侧和可见边界，再写画左/画右；切镜后不凭空交换区域 |
| 动作因果 | 接触前 → 接触 → 转换中 → 完成后；关键动作和完成态都在实际构图内可见 |
| 人物路线与道具路径 | 分开描述人走哪里、道具在哪里、何时发生变化；二者不是同一条抽象箭头 |
| 相邻片段 | 选定的景别/角度变化、动作重叠或视听线索如何接住上段；保持造型、位置、道具与光声状态，或明确合理时间跳跃 |
| 跨场或跨集状态 | 门的开合、胶带有无、衣服归属等何时建立、持续、撤销；未确定的后续写为未知，不擅自补剧情 |

普通对话只记录相关事实，不强套复杂动作表。先考虑通过机位、调度、时间和剪辑降低难度，再考虑增加参考图或白模。复杂动作可独立成单元，前后对白保留完整交流。

生产正文里的抽象词不能单独承担执行责任。“心软、愤怒、紧张、电影感、缓慢推近”等可以保留为意图，但同时要写出当前镜头真正需要的可见动作或相机机制：表演写触发后的呼吸、肌肉、手势、视线及终态；运镜写起始位置、物理侧、相对主体路径、速度行为和落点。关系已经清楚时不强填数字，也不靠堆参数压缩模型自由度。

有实际情绪转折时，按 `触发 → 可见身体反应 → 台词或动作峰值 → 残留／回收状态` 设计。残留状态可与听者反应、声音或剪辑出口并行，不强制新增镜头、停顿或秒级小节；但台词结束不能让呼吸、手势、下颌、视线等无因归零。戛然而止、主动伪装或外力打断属于有效选择，须在正文中写明中断机制。

复用旧片必须服从当前剧情和可用接镜，不为迁就旧素材改坏新设计。同步核对整场总时长，避免每次加停顿、走路和展示而持续偏离目标。

**进入素材/执行稿前应已明确：** 本段观众看见的变化、必要动作、完成态及接镜。已有设计与素材相互核对；发现具体冲突就局部回修，不反复重做整场。

<a id="reference-compatibility"></a>
## 5. 使用最小且相容的参考

创建项目文件前，按根 schema 确认 `materialType / subject / targetDir / version / storyBinding`。所有生产依赖必须位于当前项目内；跨项目复用按根规则复制、校验和项目化命名。

1. 实际打开参考，对照消费者的起始状态与计划机位，核对身份、完整造型（包括鞋）、人数、手中物、道具数量/开合、家具尺度、空间地标、光向及文字。问“模型继承图中这些事实，本镜会不会穿帮”：工作装高跟鞋图不能仅靠正文变成工作装拖鞋图，开门空景不能仅靠标签变成关门起点。冲突先修输入或设计，不留给试片解决；新图只在现有相容参考与合理调度不足时准备。人物标准图的四个视图必须一致；“只参考脸”不能屏蔽错误像素。空房母版不证明继承了上一段成片的人物姿态、道具和曝光。
2. 每份参考明确一个主要职责、具体消费者和当前状态。上传、登记、有哈希或曾获接受均不证明适合新的用途。换机位、可见造型或动作阶段时，只补查受影响的相容性；同图同用途且事实未变时复用检查。无消费者不生成，缺失或冲突不静默回退旧图。
3. 区分动作前后状态。地面从无胶带变为有胶带时，不能同时投入冲突背景并期待模型自动按时间切换；后续已完成态镜头使用相应状态参考。
4. 默认不用关键帧，包含真实尾帧和视频截图。确需例外，先展示实际图及用途，取得用户针对该版本和用途的明确审核后，才能作为生产参考上传、连接、同步或运行。内部 QA 抽帧不自动取得生产资格。
5. 网格、箭头、编号和说明图只用于内部沟通。不能把“成片不要出现箭头”当作消除输入污染的保障，也不能用静态标记代替动作先后。
6. 白模只表达当前最难的空间/运动事实，但不能省略因果。贴线类动作应有固定起点、卷与地面连接、移动留下的部分、断开和停止增长；不能只做人物移动加长条增长。保持无四肢示意，不以复杂模型替代正确调度。
7. 白模先正常速度播放，检查关键动作窗口和完成态，再按授权用于局部试片。白模本地通过不证明模型会继承；角色图管外观、白模管选定运动事实、文字管动作意图，实际效果仍要看生成结果。

按 [关键帧审核](.agents/skills/ai-director/SKILL.md#keyframe-approval-gate) 和根规则执行。新制作的含字图片还需逐项检查完整中文、数字与单位；单独文字层未合成不算最终素材。

<a id="image-generation-routing"></a>
### 5.1 生图工具默认路由（项目 Harness）

本表是本仓库各短剧项目生图的默认执行规则，来自用户于 2026-09-23 认可的两轮实测与持久化要求。先按图片的真实用途选工具，再决定输入方式。用户当前明确指定的工具、模型、画质、预算或纯文生限制优先；不每次重新询问选型，也不自动重做 A/B。实测依据和适用边界见 [生图工具选择与实测结论](director-knowledge-base/图片素材/生图工具选择与实测结论.md)。

| 图片用途 | 默认入口与设置 | 执行重点 |
| --- | --- | --- |
| 写实人物标准图、确有需求的多角度头像 | 官方 `libtv-cli` → **Lib Image**，`quality=high`、`resolution=4K` | 皮肤、发丝、衣料和视图一致性；每人每造型仍默认一张含正面、严格侧面、背面与头像的标准图，不自动追加头像组 |
| 大场景母版、白天或夜晚大场景 | 官方 `libtv-cli` → **Lib Image**，`quality=high`、`resolution=4K` | 建筑结构、材质、空气透视与明暗层次；昼夜对位另按下述空间检查 |
| 精致道具及道具特写 | Codex 内置 **`imagegen`** | 装饰细节与结构同时成立，检查连接、数量、开合和当前状态 |
| 白龙及类似写实奇幻生物的初始造型 | 官方 `libtv-cli` → **Lib Image**，`quality=high`、`resolution=4K` | 鳞片、鬃毛、肢体和背景；直接实测对象是白龙，其它生物属于相近用途默认选择，仍需逐张检查 |
| 已授权制作的电影关键帧候选 | Codex 内置 **`imagegen`** | 写清物理光向、面部阴影和表情；“v02”是实测提示词版本，不是模型或工具参数 |
| 上述未覆盖的新图、局部编辑或受控变化 | Codex 内置 **`imagegen`**；当前任务另有工具指定时服从指定 | 编辑前查看原图，明确只改什么、保持什么；不把初始文生图的优胜结论当作编辑效果已验证 |

**输入方式与连续性单独判断。** 两轮对比使用纯文字、零图片输入，证明的是当次观感与用途取舍，不保证人物身份或同一空间锁定。用户要求纯文生时，`imagegen` 不传 `referenced_image_paths` 或 `num_last_images_to_include`，Lib Image 使用当前 schema 对应的文生图模式并核实无图片媒体和参考入边；可以把观察转成文字，但不得偷偷传入原图或上一张生成图。生产需要身份、空间或状态连续性且允许图片输入时，从当前项目已确认的母版做有约束的编辑或派生，结果仍须检查。

**昼夜成对不等于两次独立文生图。** 实测两条路线都出现建筑、塔楼与飞艇变化。同一场景需要严格对位时，先选定母版，再在允许图片输入的范围内只改时间与光照，逐项核对机位、地标、建筑轮廓和固定物位置。若仍限定纯文生，可分别生成概念图，但有漂移就如实标记，不能宣称是同一布景的严格昼夜版本。

**工具路由不改变关键帧审核。** 仍默认不用关键帧；本表只决定已授权候选用什么生成。用于视频前，必须展示实际图片及具体用途，并取得用户针对该版本和用途的审核，不因采用推荐工具而自动成为视频输入。

执行沿用现有素材计划、提示词和生产记录，不新增审批表：

1. 确认资产用途、消费者、输入方式、画幅、数量与当前授权；按根 schema 定目录和新版本，写好提示词。默认每项一张，人物标准图的四视图在同一张内；仅生成当前确有需求的资产。写实光影用光源方向、遮挡、反射和材质响应表达，不靠堆“电影感、超清、真实毛孔”。
2. 读取所选工具的 Skill。Lib Image 路线用已安装的 `libtv-cli` 和当前 CLI 帮助查询模型/schema，核实模型、模式、`high / 4K`、画幅、数量与引用能力。远端变更仍遵守本地先更新、来源绑定及适用的 API/页面回读顺序；未生成的图片或 Node 如实保留待完成状态。不得沿用无关画布或只从节点显示名推断上游模型。能力缺失时说明具体限制，不静默降档、换模型或改变输入方式。
3. Lib Image 节点运行前回读提示词、模型设置、实际媒体和入边，与本地记录一致后再运行。`libtv node ... --run` 按当前 CLI 契约等待终态，不额外轮询或将其当成仅提交任务。`imagegen` 路线直接调用内置工具，不冒充可指定底层模型或真实 4K 参数；保存实际提示词和返回记录。
4. 下载或复制原始输出到项目标准目录，记录实际像素尺寸、版本、工具/可核实模型和来源。`4K` 是 Lib Image 请求档位，不能代替文件尺寸；比较材质时同时看原图与相近显示尺寸，不把像素更多直接当作更真实。逐张打开检查，再按用途完成正式绑定、API/页面回读；研究图保持研究用途。生成、登记、视觉检查与人工接受分别报告，未通过人工验收不得标为 `ACCEPTED`。

## 6. 一次检查完成本地正式更新，再同步 LibTV

新写或语义修改的生产视频提示词使用 [video-shot-prompt-v2](.agents/skills/doubao-creative-studio/assets/templates/video-shot-prompt-v2.md)。根规则限定的 H3 纯文生非生产选音实验沿用其专属 SOP，不为套用生产模板添加虚构引用。每个生产生成单元的正文必须独立包含必要事实、动作、逐字对白、听者反应、声音和结束状态，不能依赖模型看不到的设计表。

**单层镜头格式（当前用户标准，替代此前镜内时间分段规则）：** 开头用一行加粗摘要说明总时长、画幅、声音与视觉风格、场景约束，引用逐项列明。每个真实镜头只出现一次累计时间范围，标题为 `### 0.0s–2.0s｜镜头1：镜头标题`，下方固定三个字段：`**画面提示词：**`、`**镜头：**`、`**音效：**`。画面写完整连续动作、逐字对白和听者反应；镜头写焦段、景别、构图与实际运镜；音效写环境、同步动作声及画外声关系。镜内禁止再列时间段、秒数节拍、子镜头或对白时间表。长镜头、微表情和多轮问答可在同一镜头自然展开，不把动作步骤或每句对白机械地改名为新镜头。

需要独立看反应、道具或信息变化时，设计真实的景别、视点或主体切换，再给新镜头独立标题；不能只拆编号却沿用连续画面。用户要求重设计时可调整镜数，关联分镜总表与映射必须同步，旧版及原验收记录保留，新设计不自动继承人工通过。每个片段从0.0s累计，切镜不归零；所有镜头连续覆盖总时长，无空档、重叠或越界。示例只决定格式，不照抄蛋糕店、9.8秒、35/50mm、手持或胶片颗粒。用 `format: "single-level-shots"` 执行校验，再通读动作因果、对白与接镜。此前 `timedBeats: true` 镜内分段仅供历史稿回查，不再用于新写和本次返修。具体见 [镜头块规范](.agents/skills/ai-director/references/shot-block-format.md)。

作者最终通读就是本次 [主会话检查](.agents/skills/ai-director/references/review-mode.md)，同时核对姓名、对白、格式、时间、参考、抽象词的可执行机制和情绪峰值后的可继承状态。区分确定性校验、导演可行性判断与待试片观察：正文/哈希一致不证明参考相容，动作条目齐全不证明表演装得下。结构校验器只证明其实际覆盖的条件，不能证明剧情或媒体质量。新批次覆盖全部新内容；局部修复只查改动及受影响接镜，无重大问题即继续。

按 [本地优先发布顺序](.agents/skills/ai-director/references/workflow-contract.md#local-first-libtv-order) 执行：

1. 先保存标准目录中的新版正式稿，更新适用索引、绑定、引用计划与哈希，保留旧文件。通过本地项目/分集 API 及实际页面确认当前内容能打开。
2. 新画布没有 Node 时，先用已核实资产的逻辑 Mixed 映射，标记节点待绑定；不能编造 Node，也不能为拿 ID 提前上传或建空画布。已有已检查成果核验后复用。
3. 完成本地更新后，才按授权创建画布、上传参考和创建节点。取得真实映射后形成新版本地执行稿、更新正式绑定并核对页面，再同步。视频节点必须经统一门禁创建；同步正文前先运行本地 `check`，同时验证剧级画幅与正式台词合同。不得在上传脚本里临时改比例或改词。
4. 文字节点与视频节点 `data.params.prompt` 均从同一份本地执行稿提取。等价 Node 替换只做一致性校验；真实内容或引用职责改变才触发受影响范围的语义检查。

批量新稿先完成该批本地检查，再远端写入。具体模型、媒体引用或 Node 尚未就绪，只阻塞依赖它们的执行，不否定准确标明条件的文本交付。

## 7. 每次运行前的输入检查与试片策略

下列条件在受影响的每次运行前都必须满足，不能沿用一次旧回读代表之后所有运行：

- **画幅符合剧级锁：** 创建和每次运行均由 `scripts/video-format-gate.py` 校验项目锁、正文和节点实际参数；不能只检验错误参数彼此一致。
- **对白符合用户确认稿：** 内置 `scripts/dialogue_gate.py` 校验正式合同、剧本哈希、确认来源、单元台词覆盖、说话人、逐字正文和顺序；缺失、冲突或擅改均在付费提交前阻断。详见 [门禁字段与接入](docs/VIDEO-PRODUCTION-GATES.md)。
- **入口正确：** 实际模型、模式、时长、分辨率、声音开关和参考能力符合当前执行稿；限制来源可追溯，不能只看模板默认值。
- **媒体合规：** 按实际待提交列表分别检查各类型数量、总数量、单项时长与累计时长。参考音频时长用文件实测后求和；输出视频时长、音频条数和参考音频总时长是不同约束，不能互相替代。限值来自当前入口 schema、官方说明或明确报错，不把某次入口的15秒推广成通用规则。已有校验器覆盖的项目自动检查；未覆盖的补做测量与比较，在现有运行记录记实测值、限值及来源，不能只写“CLI通过”。
- **所有引用一致：** 检查当前 schema 使用的类型列表、混合参考列表及实际入边，无重复、旧引用、错版本或冲突状态。只换了 `audioList` 不代表混合列表也已更新。
- **三处正文一致：** 按统一正文提取规则比较本地正文、文字节点、视频节点，记录三者 SHA-256；媒体与 `referencePlan` 一致。差异未解释并修复前不运行。
- **授权与依赖满足：** 本次范围、预算、用户暂停要求、关键帧例外审核和必须的上游状态都已满足。未通过的错误尾帧不得继续传给下游。

<a id="risk-trials"></a>
已知错误在此拦截；对尚无具体输入冲突、只能通过生成判断的自然表演或模型行为，使用获授权的试片，不无限扩写提示词。在现有执行备注明确“哪个片段检验什么、看见/听见什么才成立、哪些后续依赖此结果”。只有“高风险，待试片”而没有对应消费者，不能作为扩量依据。优先验证当前最不确定的声音组合、接触动作、空间接续或特殊运镜；一个适用片段可覆盖多个问题，不要求每类额外生成一条。使用实际支持且获准的低成本配置，不规定所有任务必须480P。

自动顺序生产前，按根规则验证代表片：存在冷启时覆盖冷启，有对白时覆盖首个对白；新模型或新人物标准图版式也需验证。可由同一代表片覆盖多个问题，不要求逐段重复试片；已有适用且获用户确认的证据可复用。用户另有明确试片选择时按其决定执行。

同场独立生成出现新的高风险接续时，在已授权范围内优先验证相关相邻片段的实际接点，再扩量复用该接法；无关场景的单条代表片不能证明这组接镜成立。失败时暂停尚未运行的同问题消费者，修正后只复验受影响范围，无关单元继续。已有可用片段直接拿来验证，不为补流程额外生成。用户明确调整试片或批量顺序时按当前决定执行，记录未验证范围，不把批量授权写成效果已验证。

试片通过后保留方案，停止可选试验；升分辨率或重生成的结果仍须实际验收。已通过片段不因更换编号、补报告或美学偏好自动重跑。

## 8. 失败与返修：修最早出错的层

先划定返修范围。改比例、清晰度、构图、动作、口型或修穿帮时，已确认台词及说话人保持不变；有可用原声时默认保留并记录来源。生成结果说错词，要恢复确认稿，不用改合同去迎合错片。只有用户明确要求改台词才回到第3节解锁，生成或返工授权本身不包含改词授权。

| 现象 | 下一步 |
| --- | --- |
| 提交失败或通用平台错误 | 保留任务号和错误，先核对实时约束、累计参考量及残留引用；不凭通用错误直接改剧本或原样反复提交 |
| 提交结果未知、等待中或用户要求暂停 | 查已有任务避免重复提交；遵守暂停边界，说明已提交的平台任务可能继续，不擅自重跑或承诺已取消 |
| 节点中的多个引用字段不一致 | 先恢复同一已检查输入并回读；无法可靠修复时，在授权内建干净新版节点，保留旧节点 |
| 动作方向或道具因果反复失败 | 检查空间、机位、顺序动作容量和参考状态；先修设计或输入，必要时分离复杂动作或省略非剧情过程，不只堆负面提示词 |
| 单条可看，连起来跳色、跳姿势或动作断掉 | 连看接点，先修光色、动作相位和入出点；近似机位硬续失败时改有动机的景别/角度切换或动作重叠，重新判断必要的单元边界，不默认继续拆碎或强行合成长镜头 |
| 脸红/说话/拿物主体错误 | 对照当前剧本明确角色全名、动作和听者反应，修受影响片段及接镜，不改正确剧情迁就生成结果 |
| 全局音色、造型、模型或映射错误 | 暂停尚未运行的受影响下游，修共同原因并检查其消费者；无关片段继续 |
| 特殊运镜或转场未实现 | 对照预定成功标志检查原片；改变有依据的运动/输入方案，或在既有授权内采用能保留所需效果的生成与后期分工；不靠反复添加技巧名重试 |
| 微小表演、时间或审美偏差 | 记录非阻塞备注，满足当前用途就停止；不追加生成次数 |

重试必须有可说明的修复或明确的暂时性失败依据，并处于已有授权范围。持续失败且没有可行修正时报告受阻，继续不受影响的工作，不把无限重试当成完成任务的方法。

<a id="repair-window"></a>
**局部返修的最小上下文是受影响的接镜窗口。** 有实际选片时，写新版前先连看“上一段有效尾部 → 本段 → 下一段有效开头”，用真实姿态、站位、道具与声音确认故障；修后重看同一窗口。只改鞋也要检查改动是否影响弯腰起点、取物位置或离场完成态。邻段尚未生成时核对当前设计并保留接点待验，不猜理想尾帧；查看/抽帧用于分析不授权将帧送入模型。未受影响的接点复用既有检查，已获用户接受的邻段优先保留。

## 9. 媒体验收：逐段看听，再检查接镜

下载按根规则取得真实无水印原链，核对文件完整性并检查实际画面四角。平台成功、解码、哈希、缩略图、自动转写或无水印参数均不能替代真实媒体检查。

人物标准图须同时检查大头像和全身正面、侧面的小脸：记录实际像素尺寸，放大检查面颊杂纹、五官边界、身份及造型一致性。高分辨率标签不能代替真实细节，缩略截图也不能证明同尺度质量；清晰度修复后须另查眼唇、脸型和服装是否被重绘。未完成原尺寸检查时明确记录范围。比较生成路线时保留同源输入与提示词，说明实际尺寸差异；候选未选定前不替换已用人物参考。

1. **每条当前选片正常速度完整播放并试听。** 先看人物/造型、人数、动作归属和剧情，再查空间因果、道具及明显视听破坏。对白对照合同，听清增删词、说话人、起音尾音、噪声和突兀静音；自动转写只能辅助。
2. **状态变化加查连续窗口。** 开门、接箱、贴线、挂衣等检查接触前、接触、转换中和完成后，配合足够密集的帧。不能只凭首中尾三帧判断成功。
3. **按播放顺序连看相邻片段。** 核对人物、光色、空间方向、道具状态、声音和句子完整性；使用实际入出点或临时粗排，动作重叠须去掉重复部分后再判断是否卡顿、重做或跳过动作。不要求额外完成未授权的精剪。单条分别通过不等于组接通过；完整素材包交付前覆盖所有实际接点，只交局部时明确覆盖范围。
4. **剪辑或补镜后重查受影响窗口及声音。** 裁去模型多说的话不能伤到正常对白；裁切隐藏错误不得破坏必须看懂的信息，更不得用于遮盖水印。不能只检查修补前原片。

验收采用根规则的三级标准：

- `HARD_REJECT`：人物/动作主体错误、跨错边界、道具凭空变化、明显穿模、改变剧情的错误对白、损坏、水印或明确硬约束不满足。标明具体时间点与影响，停止该片进入剪辑并局部返修。
- `PASS_WITH_NOTES`：故事、关系和剪辑成立，仅有不影响叙事的小偏差。可按当前用途继续，不自动提升为人工 `ACCEPTED`。
- `OPTIONAL_POLISH`：理想表情、厘米级站位、逐秒动作等审美精修。无明确要求时不阻塞、不自动重跑。

无法实际播放或试听时明确报告“未检查”及范围，不把机器结果称为媒体通过，也不把基础 QA 静默转交用户。“没有大问题就通过”不豁免错人、错词、裁音、空间和道具硬错误。用户已通过具体版本和用途后停止可选返修；只有新的明确硬错误证据才重新提出。

## 10. 正式接入、顺序交付和收尾

1. 当前选中的素材落在项目标准业务目录，登记稳定资产 ID、完整归属、相对路径、版本、状态与 SHA-256；通过项目/分集 API 和页面回读。运行证据留在生产记录，不能代替正式绑定。
2. 在现有当前执行目录统一指定选片、播放顺序和备注，直接消费者与索引一起更新，并按[当前状态规则](#current-production-state)移除新版说明中的过期结论。按明确 `order` 排序，不按历史 shot ID 猜顺序；历史试片和旧版默认保留，清理另需授权。
3. 用户要求审查包时，复制当前选片到一个版本化交付目录，用 `01、02……` 加内容、稳定主体和版本命名，核对数量、时长和哈希。不要修改源素材文件名或把所有试片混入当前选片。
4. 简洁分开报告：正式成果及本地接入、运行证据、实际画面/试听/接镜覆盖、未完成项、人工接受状态。9段素材齐全不等于整集已剪完；用户明确已剪完或通过时，记录该确认来源和范围，不冒充亲自看过最终成片。

## 11. 三个动作前的最后自查

这些问题在对应步骤内完成，不增加一轮审查或新表单；在现有记录里留一条简短结论与具体异常即可。

| 即将执行的动作 | 必须能回答的问题 |
| --- | --- |
| 写远端画布/节点 | 当前剧情和对白是哪版？动作/反应归属、空间与参考有无冲突？本地正式版本、索引、API和页面是否已更新？ |
| 运行试片/正片 | 授权和必要用户审核是否满足？实际模型约束、所有媒体列表、入边及三处正文是否一致？已知错误是否清除？ |
| 放入选片/交付 | 文件与真实无水印原链是否核对？实际播放、试听和相邻接镜是否检查？当前选片与索引是否一致？报告是否区分生成、接入、媒体检查和人工接受？ |

只拦截受问题影响的动作。已通过且未变化的内容继续复用；后续执行条件尚未齐备时，仍可完成准确标明边界的文本工作。
