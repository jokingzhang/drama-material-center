# AI 短剧素材中心操作规范

## 1. 项目定位

本项目是一个仅在本机运行的多项目短剧素材浏览器，由两部分组成：

- Git 仓库：前端、Vite 本地服务、测试、配置样例和审计脚本。
- 本地工作区：每部短剧的配置、封面、剧本、图片、音频、视频和生产资料。

两者必须严格分离。除 `workspace/.gitkeep` 外，任何项目数据或素材都不得进入 Git。

默认工作区是仓库内的 `workspace/`。如果存在 `.env.local`，先读取 `MATERIAL_CENTER_WORKSPACE`，相对路径以仓库根目录为准。执行资源操作前必须确认实际工作区，不能想当然地操作默认目录。

## 2. 不可破坏的原则

1. 先读当前真实状态，再操作：至少检查 `git status --short --branch`、工作区配置、目标项目的 `project.json` 和目标目录。
2. 用户没有明确要求时，不移动、重命名、覆盖或删除已有素材，不批量整理目录，不清理旧版本。
3. 迁移默认采用“复制、校验、保留源目录”，不得用移动代替复制。删除源资源必须另行获得明确授权。
4. 不因“文件已生成”“接口返回 200”“任务完成”就宣称素材可用。图片要实际查看，视频要实际播放；生成、保存、登记、视觉验收和成片可用是不同状态。
5. 不把分镜板、带网格/箭头/编号/说明文字的动作板直接当作视频生成输入。内部参考与干净生成输入必须分开标记。
6. 资源任务只处理用户点名的项目、集数、镜头或目录。不要顺手修改其他项目或历史资产。
7. 本项目没有删除项目的接口。不要为了方便自行增加或调用删除逻辑。
8. 本地服务能读取真实素材，不得未经授权绑定到 `0.0.0.0`、暴露到公网或部署到外部环境。

## 3. 项目和资源结构

每个可被应用发现的项目必须使用以下结构：

```text
<workspace-root>/<project-id>/
├── project.json
├── cover.png                  # 可选，也可为 jpg、jpeg、webp、gif
└── library/                   # 只有这里会被素材中心扫描和展示
    ├── 剧情/
    ├── 图片/
    │   ├── 人物/
    │   └── 场景/
    └── 视频/
        └── 成片/
```

项目可以在 `library/` 之外保存本地生产资料，例如参考资料、实验包、提示词或第三方工具状态；它们仍受 Git 忽略规则保护，但不会出现在素材中心页面。除非用户明确要求迁移或重构，不要把这些既有目录强行并入 `library/`。

### 3.1 项目素材 schema（创建前必读）

根 `AGENTS.md` 是所有短剧项目共用的素材 schema；项目 `PRODUCTION_RULES.md` 只补当前剧的事实、选择、特例和验收状态，Skill 只说明怎样创作、生成或验收。三者不得各自发明一套目录。

Agent 创建任何项目文件前，必须先列出并确认下面五个字段：

```text
materialType  产物类型
subject       项目 / characterId / lookId / episodeId / sceneId / shotId / locationId / propId
targetDir     按下表得到的唯一默认目录
version       本次新版本，使用 vNN 且不覆盖旧版
storyBinding  是否以及挂到哪个剧本、角色、分集或场次对象
```

如果产物不属于任何已知 `materialType`，先提出 schema 增补建议；不得临时创建新的顶层目录或“其他 / 杂项 / 暂存 / final-final”等同义目录。

项目根目录职责固定为：

```text
<workspace-root>/<project-id>/
├── project.json
├── cover.png | cover.jpg | cover.webp | cover.gif
├── PRODUCTION_RULES.md             # 可选；当前项目事实和特例
├── production/                     # 只放剧本业务索引和可重建报告
│   ├── story-index.v1.json
│   └── asset-bindings.v1.json
├── library/                        # 正式人读文档与可浏览、复用的项目素材
├── 创作记录/                       # prompt、job、raw response、selected run 等创作证据
├── 生产记录/                       # 媒体任务、下载、解码、抽帧和 QA 证据
├── 交付包/                         # 经明确交付动作形成的副本和 manifest
└── 回收站/                         # 获得整理授权后的可恢复旧文件；日常不向这里生成
```

新的真实项目成果不得写入仓库 `docs/projects/<project-id>/`；仓库 `docs/` 只放产品 PRD、架构和程序维护文档。原始响应、抽帧和 QA 证据不得进入剧本阅读主线，参考媒体默认不满足正式业务需求。

| `materialType` | 业务主体 | 默认目录 | 是否进入剧本视图 |
| --- | --- | --- | --- |
| `story.source` | 全项目 | `library/剧情/统一/` | 是，作为当前剧本或大纲来源 |
| `story.summary` | 全项目 | `library/剧情/统一/` | 是，展示故事大概和确认状态 |
| `story.character-setting` | `characterId` | `library/剧情/角色/<CHAR-ID-姓名>/` | 是，与角色图一起展示 |
| `story.episode-script` | `episodeId` | `library/剧情/分集/<EP>/剧本/` | 是，作为分集正文或原文定位 |
| `plan.asset-project` | 全项目 | `library/剧情/统一/素材计划/` | 是，用于全剧素材计划和缺口 |
| `plan.asset-episode` | `episodeId`，可选 `sceneId` | `library/剧情/分集/<EP>/素材计划/` | 是，用于解释当集需求和缺口 |
| `plan.shot` | `episodeId / sceneId` | `library/剧情/分集/<EP>/分镜/` | 是，作为场次上下文，不抢阅读主线 |
| `contract.dialogue` | `episodeId / sceneId / characterId` | `library/剧情/分集/<EP>/分镜/` | 是，在场次和声音缺口中展示 |
| `prompt.image` | 角色、场景或道具主体 | 与目标图片相同的业务目录 | 从素材来源打开，不当作图片 |
| `prompt.video` | `episodeId / sceneId / shotId` | `library/剧情/分集/<EP>/分镜/` | 是，与镜头上下文展示 |
| `prompt.voice` | `characterId` | `library/音频/人物/<CHAR>/` | 从声音来源打开，不当作声音本身 |
| `image.character` | `characterId + lookId` | `library/图片/人物/<CHAR>/<LOOK>/` | 是，挂主造型、备选或剧情必需造型 |
| `image.scene` | `locationId` | `library/图片/场景/<LOC>/` | 是，挂场次场景素材 |
| `image.prop` | `propId` | `library/图片/道具/<PROP>/` | 是，挂角色或场次道具 |
| `image.derived` | `sceneId` 或 `shotId` | `library/图片/剧情/<EP>/<SCENE>/` | 默认折叠，不进基础完成度 |
| `audio.voice` | `characterId` | `library/音频/人物/<CHAR>/` | 是，挂角色声音 |
| `audio.scene` | `sceneId` | `library/音频/剧情/<EP>/<SCENE>/` | 是，挂对白或时间轴音频 |
| `audio.ambient` | `locationId` | `library/音频/环境/<LOC>/` | 是，在场次素材中展示 |
| `audio.bgm` | 全项目或 `episodeId` | `library/音频/BGM/` | 是，在项目或分集声音区展示 |
| `video.shot` | `sceneId + shotId` | `library/视频/剧情/<EP>/<SCENE>/` | 是，作为二级镜头素材 |
| `video.final` | `episodeId` | `library/视频/成片/<EP>/` | 是，验收状态与基础素材齐套分开计算 |
| `media.reference` | 可选业务主体 | 对应 `library/图片/参考/`、`音频/参考/` 或 `视频/参考/` | 否，默认不满足正式需求 |
| `record.creative` | workflow / run | `创作记录/<agent>/<workflow>/<run-id>/` | 否，只从来源追溯中打开 |
| `record.production` | media job / QA | `生产记录/<purpose>/<scope>/<job-id>/` | 否，只从验收证据中打开 |
| `delivery.package` | delivery ID | `交付包/<delivery-id>/` | 否，只在交付记录中展示 |

标准 `library/` 下级目录为：

```text
library/
├── 剧情/
│   ├── 统一/素材计划/
│   ├── 角色/<CHAR-ID-姓名>/
│   └── 分集/<EP>/
│       ├── 剧本/
│       ├── 素材计划/
│       └── 分镜/
├── 图片/
│   ├── 人物/<CHAR>/<LOOK>/
│   ├── 场景/<LOC>/
│   ├── 道具/<PROP>/
│   ├── 剧情/<EP>/<SCENE>/
│   └── 参考/
├── 音频/
│   ├── 人物/<CHAR>/
│   ├── 剧情/<EP>/<SCENE>/
│   ├── 环境/<LOC>/
│   └── BGM/
└── 视频/
    ├── 剧情/<EP>/<SCENE>/
    ├── 参考/
    └── 成片/<EP>/
```

命名使用“稳定主体 + 产物职责 + `vNN`”，例如：

```text
STORY-SCRIPT-红果32集-v01.md
CHAR-001-SETTING-v01.md
EP01-SCRIPT-红毯成空-v01.md
EP01-S03-SHOT-PLAN-v01.md
CHAR-001-LOOK-JQ-EP01-PLANNER-STANDARD-v01.png
LOC-WEDDING-BACKSTAGE-MASTER-FRONT-v01.png
PROP-ART-PORTFOLIO-STANDARD-v01.png
EP01-S03-U04-KF-v01.png
EP01-S03-U04-VIDEO-v01.mp4
EP01-FINAL-v01.mp4
```

- `DRAFT / ACCEPTED / REJECTED / SUPERSEDED` 等可变状态写入业务索引，不写入新文件名；状态变化不得靠重命名或再复制一份表达。
- 新版本永不覆盖旧文件；当前版本由索引中的稳定 `assetId` 或文档绑定指向，不创建 `final`、`最终版2` 等名称。
- 能出现在剧本、角色、分集或场次页的成果必须绑定稳定业务 ID；物理目录和中文标题都不是业务主键。
- 历史非标准路径先在 `story-index.v1.json` 或 `asset-bindings.v1.json` 中以 `legacyPath: true` 原地映射。页面上线不授权移动、改名或删除；实体迁移必须另开任务，先复制校验，默认保留源文件。
- 任务结束时分开报告正式成果、运行证据和索引更新，不能用一句“文件已生成”混合三种状态。

导演知识库是独立的仓库级空间，网页只读，事实源固定为仓库根 `director-knowledge-base/` 下的 Markdown 文档。它只按 `剧本/`、`图片素材/`、`分镜提示词/` 三个问题域组织，由项目级 AI Director Skill 按当前任务渐进读取和维护；不要求 JSON 索引、知识 API、成熟度系统或 AI 使用流水账才能工作。案例和来源只作为需要时查阅的研究材料，不能覆盖当前项目事实与用户决定。不得扫描、合并或删除工作区里同名的历史素材目录；它们继续按普通项目文件处理。

`project.json` 的有效格式为：

```json
{
  "schemaVersion": 1,
  "name": "项目显示名称",
  "description": "可选说明",
  "cover": "cover.png"
}
```

- `<project-id>` 是稳定标识，同时用于文件夹名和 URL。只允许小写字母、数字和连字符，长度不超过 64 位；创建后不要随意改名。
- `name` 是可修改的显示名称，不承担稳定标识职责。
- `cover` 只能指向项目根目录中的 `cover.png`、`cover.jpg`、`cover.jpeg`、`cover.webp` 或 `cover.gif`。
- 封面优先通过页面上传或更换；仅支持 PNG、JPEG、WebP、GIF，最大 10 MB。推荐使用 16:9 横图。
- 应用会忽略隐藏文件和符号链接。不要用符号链接绕过项目边界或把外部目录伪装成项目素材。
- 当前网页可直接预览 Markdown/文本、图片、视频和音频；文件夹缩略图中的音频按用户点击后加载。网页可播放不等于已完成内容验收，生产验收仍须实际试听并记录范围。

## 4. 资源管理 SOP

### 4.1 新建项目

1. 启动 `npm run dev`，优先从首页“新建项目”创建标准骨架。
2. 选择稳定的英文 kebab-case 项目标识；项目名称和说明可使用中文。
3. 只按 3.1 节的素材 schema 补充目录，不另建同义分类。例如分集文档进入 `剧情/分集/<EP>/`，角色或具名怪物都以稳定 `characterId + lookId` 进入 `图片/人物/<CHAR>/<LOOK>/`，场次声音进入 `音频/剧情/<EP>/<SCENE>/`，分镜视频进入 `视频/剧情/<EP>/<SCENE>/`。找不到 `materialType` 时先提 schema 增补建议。
4. 上传封面后刷新首页，确认封面、名称和项目链接都正确。
5. 立即运行 `npm run audit:git-boundary`，确认新项目完全处于 Git 忽略范围。

### 4.2 导入或迁移资源

1. 明确源目录、目标项目和目标相对路径；先记录源端文件数和总字节数。
2. 确认目标目录位于实际工作区的目标项目内，且 `git check-ignore` 能命中一个目标样例文件。
3. 大批量资源使用可校验的复制方式，例如 `rsync`；默认排除 `.DS_Store`，不要改动源目录。
4. 复制后至少比较：
   - 文件数；
   - 总字节数；
   - `rsync --archive --checksum --delete --dry-run` 或等价的双向完整性校验结果；这里只允许使用 `--dry-run` 做差异检查。
5. 在页面中抽查 Markdown、图片、视频和音频；关键音频验收仍应完整试听。深层目录 URL 刷新后仍应停留在原项目和原目录。
6. 再运行 Git 边界审计。只有复制校验、页面抽查和 Git 边界都通过，才能报告“迁移完成”。

统计时只排除明确不迁移的系统文件。不要用过宽的文件名规则误排除资源包内部同名文件。

### 4.3 新增或生成素材

**生图统一使用 Codex 内置 `imagegen` 生成或编辑本地图片。** 不得用 Python、一次性脚本或 CLI/API 调用替代常规生图流程；只有用户明确指定或确认 CLI/API 模式时才允许切换。编辑本地图片前先实际查看原图，最终选中的项目图片必须复制或移动到目标项目资源目录，并使用版本化文件名避免覆盖已有资产。

**每个具名人物的每套可见造型，默认只做一张人物标准图。** 同一张图必须同时包含全身正面、严格侧面、全身背面和头像特写；四个视图的脸、服装、发型、饰品、年龄感、妆容与伤病状态必须完全一致。角色换装、换发型或状态明显变化时另做一张对应标准图，不得把不同造型拼进同一张图，也不得出现“头像是 A 服饰、三视图是 B 服饰”。复杂接触、特殊姿态、多人站位或关键道具关系另外补关键帧，不把这些职责硬塞进人物标准图。

1. 先读当前剧本、导演说明、角色/场景母版和已有素材清单；当前文件是事实源，旧对话或旧报告只能作线索。
2. 一次处理一个明确资产或一个明确镜头。单项不合格时只修该项，避免盲目批量重生成。
3. 文件名应能稳定表达集数、镜头/资产、用途和版本，例如 `EP08-S12-KF-GEN-v02.png`。不要覆盖上一版；确认替代关系后再在清单中标为 `SUPERSEDED`。
4. 如果项目已有素材清单，新增资产必须登记：最终相对路径、用途/责任、上游母版或来源、状态，以及必要的版权/来源信息。
5. 建议在清单中使用清晰状态，而不是靠口头判断：
   - `DRAFT`：草稿，未验收；
   - `INTERNAL`：动作板、白模、平面图等内部参考；
   - `GEN_INPUT`：已清理、允许直接用于生成；
   - `ACCEPTED`：已实际查看或播放并通过当前用途验收；
   - `REJECTED`：不可使用；
   - `SUPERSEDED`：已被新版本替代。
6. 已上传到第三方画布不等于已登记或已验收；外部工具中的项目 ID、画布 ID 和当前本地项目标识也不能互相猜测。

### 4.4 验收资源

- 图片：逐张打开，检查主体一致性、肢体/道具、文字污染、构图、比例以及与用途是否匹配。
- 视频：逐段实际播放，检查动作因果、连贯性、首尾帧、闪烁/畸变、字幕/音画和是否可拼接。
- Markdown：检查正文、相对图片链接和项目级文件 URL。
- 音频：实际试听，检查内容、音色、噪声、截断、时长和对应镜头。
- 只有与当前任务相关的资源可以计入验收范围；不确定或待试片的资源保持条件状态，不能为了“清单完整”而伪报通过。

### 4.5 统一视频提示词与 LibTV 写入闸门

- 除下述“H3 纯文生固定音色 SOP”明确限定的非生产选音实验外，所有豆包视频分镜和生成提示词统一使用 `doubao-creative-studio` 的 `video-shot-prompt-v1`，不因目标模型不同拆模板；后续只有用户明确需要时再区分。模板必须在豆包创作时使用，不做二次创意改写，也不为凑字数扩写。
- 本项目不设固定的最终提示词字符上限。豆包 CLI 的传输安全线和目标视频入口的真实限制按 Skill 处理；超限时交回豆包做新版本，不由 Codex 压缩正文。
- 除下述 H3 纯文生选音实验明确要求零引用外，写入 LibTV 的任务必须在 `referencePlan` 中预先使用已核对的一对一 `Node` 引用；映射缺失、不唯一或结构校验失败时停止，不得写入。
- 通过校验的本地统一执行稿是唯一写入源；对应文字节点与视频节点的 `data.params.prompt` 必须来自同一内容。批量任务先全部本地校验，再远端写入。
- 写入后同时回读文字节点和视频节点，与本地执行稿逐项比较；全部一致后才能报告“LibTV 提示词同步完成”。同步不授权运行视频节点。

#### 人物标准图生产闸门

- 每个有可见具名人物的生成单元都先确定“角色 + 当前造型/状态”，并找到与之完全匹配的一张人物标准图；不同造型或明显状态变化使用各自的标准图。
- 标准图必须同时包含全身正面、严格侧面、全身背面和头像特写。使用前实际查看四个视图，确认身份、脸、服装、发型、饰品、年龄感、妆容和伤病状态一致；任何不一致都先修图，不让模型自行选择。
- 人物标准图负责身份、外形和当前造型。多人站位、肢体接触、特殊姿态、剧情道具关系、复杂构图或首尾状态另外使用关键帧；关键帧不能反过来替代人物标准图。
- 参考越多不等于越稳定。每个镜头只接当前任务需要且彼此兼容的最小参考集合；写入前按实时模型 schema 校验图片、音频、视频和总媒体上限，超限或冲突时停止并重新设计参考集合。
- 素材出现在画布图片栏不等于已经参与生成。写入和交付前仍要逐镜核对本地引用计划、执行稿说明、视频节点媒体列表与实际入边。
- 新模型或新标准图版式在批量运行前，必须用代表性镜头试片，并实际播放检查身份、体型、转身、服装连续性以及重复人物或拼板污染。节点装配或回读通过只证明引用已写入，不证明成片可用。

#### LibTV 视频下载与无水印交付闸门

- 从 LibTV 下载、导出或回收的视频，在进入当前选片、剪辑清单、完整成片或用户交付前，必须取得视觉上无水印的原链。左上“AI生成”、右下 `LibTV`、平台 Logo、角标或其它可见水印任一存在时，状态必须为 `REJECTED_WATERMARKED`，不得登记为 `GEN_INPUT`、`ACCEPTED`、可剪辑片段或成片。
- 不得把普通 `libtv download`、直接下载节点 `url`或只传 `--without-ai-watermark` 的结果默认当成无水印交付。当前 CLI 与账号具备原链权限时，使用 `libtv download --without-ai-watermark --vip ...`；`--vip` 只能在已确认当前账号具有对应权限时声明。已观测到仅使用 `--without-ai-watermark` 可能仍保留右下 `LibTV` 标识，或返回与带水印文件字节完全相同的视频。
- 下载后必须登记画布、节点、任务、远程 URL、实际下载命令、本地路径与哈希；完成媒体全流解码，并实际查看原尺寸首帧、中段、尾帧和四角。完整拼接片还必须查看代表性接触表及所有剪辑点前后帧。只有这些视觉检查通过后，文件名、清单和 QA 状态才能写“无水印”。
- 如果“无水印”下载件与已知带水印文件哈希相同，或实际查看仍有任何水印，立即停止交付；过程副本只能作为 QA 证据保留，不得进入当前选片或成片。不得用裁切、模糊、遮盖、贴图或二次编码伪装成无水印；必须改取真实无水印原链，或从已验证的无水印片段在本地版本化组装。
- 平台或当前账号无法提供无水印原链时，必须报告受阻与已尝试的下载链路，不得把带水印视频作为用户交付物。视频内嵌的 AIGC 元数据不等于可见水印，两者要分开登记。

#### 台词、声音与连续性生产闸门

- 任何含台词的视频在运行前都必须冻结集级台词合同。每句台词至少记录镜头、说话人、表达意图、逐字正文、时间区间、声音引用和嘴部是否可见；逐字正文必须同时出现在该镜头的时间轴和声音说明中，不能只写在声音区或剧情摘要里。
- 声音引用只承担明确登记的音色或对白职责，提示词和节点映射都必须显式点名。目标模型需要时间轴音频时，先把短对白用静音补齐到节点时长，或按模型要求合成为单条时间轴音频；不得靠拉伸、截断或未声明的替换来“凑时长”。
- 当前镜头使用的人物标准图必须与本场可见服装、发型和状态一致；如果还接入关键帧或其它人物参考，所有可见信息必须相容。多个引用冲突时先停止生成，不能指望模型自行选择正确版本。
- 若缺陷会影响多个镜头，例如台词未进入时间轴、全局声音或服装错误、模型选错、引用映射错位，应立即停止尚未运行的下游节点，修复并重新校验整批执行稿；只有明确局限于单镜头的问题才只重做该镜头。
- 自动顺序生产前，至少先验证代表性试片：项目存在冷启时覆盖冷启，存在对白时覆盖首个对白；声音、口型、服装连续性、剧情钩子与镜头语言均符合当前方向，并获得用户确认后，才可继续批量运行。机器检查通过仍不等于用户验收。

#### H3 纯文生固定音色 SOP

- 只有在任务目标明确是为角色建立可复用的 H3 原生音色锚点时，才允许把纯文生视频当作选音容器。该视频统一标记为 `INTERNAL_VOICE_CAST_CONTAINER`，不承担角色定妆、剧情镜头、`GEN_INPUT` 或成片职责；不得为了满足普通镜头模板而伪造图片、视频、音频引用或 `referencePlan`。
- 选音提示词仍由 `doubao-creative-studio` 创作和版本化保存，使用非生产选音任务的结构化输出；单个 job 最多处理两个角色。每个角色只允许一个说话人和一句来自当前剧本事实源的逐字台词，明确角色音色、语速、重音和情绪，并明确禁止 BGM、歌声、第二人声、广播、强环境声、动作音效、混响、回声、电音、字幕和可读文字。Codex 只做事实打包、校验和执行，不二次改写豆包正文。
- 在独立且带版本号的 LibTV 画布中按 `声音 | 图片 | 文字+视频` 三栏布局；图片栏明确写“本轮纯文生无参考”。同一角色的文字节点与视频节点必须使用同一份本地统一执行稿，并在运行前回读确认提示词逐字一致、引用列表全空。每次以实时模型 schema 为准；本 SOP 的标准候选配置为 `Minimax H3`、`text2video`、`9:16`、`768P`、8 秒、`count=2`。模型缺失、schema 不匹配或回读不一致时停止，不得静默换模型、补引用或重试。
- 先完成全部本地校验，再按角色逐个运行；一个角色只提交一个产生 A/B 两版的任务，必须等 CLI 返回终态后才处理下一个。失败时保留任务号和错误证据并停止当前项；结果未知时先回读节点，确认没有远端副作用后才可在用户知情的情况下受控重试。
- 下载后保留原始 MP4，用版本化文件名复制到项目 `library/视频/参考/`，再从每条视频的原始音轨提取 PCM WAV，禁止用再次有损编码、拉伸或截断制造音色锚点。WAV 放入 `library/音频/人物/`；源视频与提取音频必须登记角色、A/B 版本、逐字台词、豆包输出、画布/节点/任务、远端 URL、本地路径、时长、格式和哈希。
- 机器验收至少包括源副本校验、视频和音频完整解码、音频参数、响度、真峰值、静音段，以及视频多时点画面检查；异常必须逐候选记录。机器通过后状态仍只能是 `VOICE_ANCHOR_CANDIDATE`，因为内容正确性、音色、情绪、噪声和失真必须由人实际试听。旧候选和旧锚点一律保留，不覆盖、不删除。
- 用户明确选择 A 或 B 后，即把被选中的确切 WAV 提升为 `VOICE_ANCHOR_ACCEPTED`；保留候选源文件不动，以无转码复制方式落到 `library/音频/人物/<角色>/` 的稳定角色资产路径，并在项目级音色资产清单登记角色 ID、版本、来源、哈希和用途。后续节点必须引用该本地文件或其经回读一致的 URL，不能用“同一模型”“同名音色”或另一条近似样本替代。不同台词复现不是定音必做项，只有用户另行要求或当前镜头生产验证确有需要时才执行；执行时回读生成节点的 `audioList` 与已选锚点逐项比对。相同声音引用只能提高一致性，不等于严格声纹保证。

### 4.6 LibTV 画布布局

- 新建或重建 LibTV 画布时，必须从左到右按 `声音 | 图片 | 文字+视频` 三栏组织，禁止依赖默认坐标把节点堆叠在同一点。
- `声音` 栏只放角色音色参考、对白、环境声和 BGM；`图片` 栏放人物、场景、道具与 `GEN_INPUT`，其中 `INTERNAL` 必须在图片栏内单独隔离并明确标注禁止接入视频生成；`文字+视频` 栏把剧本、分镜提示词放在左侧，对应视频节点放在同一行右侧。
- 当前没有某类素材时仍应保留该栏空间或明确标注“暂无”，不得用其它类型填满空栏。事实源与完整确认稿放在 `文字+视频` 栏顶部，分镜提示词按执行顺序向下排列，为对应视频节点预留固定位置。
- 创建和上传节点时必须显式指定坐标，并使用足够的水平、垂直间距避免遮挡。交付前用 `libtv project <画布UUID>` 回读坐标、节点类型和连线，确认三栏顺序正确、没有大面积重复坐标、`INTERNAL` 未误接生成链。
- 既有画布若 CLI 支持安全改位则原地重排；若 CLI 不支持移动已有节点或分组，则新建带版本号的整齐布局画布，保留旧画布，不覆盖或删除旧版本。

### 4.7 项目专属规则入口

- 处理项目 ID `limited-marriage-rivals` 的创作、素材、LibTV 或成片任务时，必须额外读取 [`workspace/limited-marriage-rivals/PRODUCTION_RULES.md`](workspace/limited-marriage-rivals/PRODUCTION_RULES.md)。若 `.env.local` 配置了其他实际工作区，则将该引用解析为 `<实际工作区>/limited-marriage-rivals/PRODUCTION_RULES.md`；目标项目存在但规则文件缺失时，停止该短剧的媒体生产并报告，不得根据旧对话自行重建规则。

## 5. 修改程序的 SOP

### 5.1 修改前

1. 阅读 `README.md`、`package.json` 和本文件。
2. 检查 Git 状态，保留所有用户已有修改；只编辑当前任务范围内的文件。
3. 先定位事实源：
   - 项目/路径安全：`server/projectWorkspace.ts`
   - 本地 API 和文件扫描：`server/materialLibraryPlugin.ts`
   - URL 编解码：`src/lib/routes.ts`
   - 项目首页：`src/pages/ProjectsPage.tsx`
   - 素材页：`src/pages/ProjectLibraryPage.tsx`
   - Git 边界：`scripts/audit-git-boundary.mjs` 和 `.gitignore`

### 5.2 实现约束

- URL 是项目、目录、选中文件、搜索和排序状态的事实源。不要把这些状态重新退回到只存在于组件内存的 `useState`。
- 所有文件 API 必须在服务端校验项目、真实路径和工作区边界；继续拒绝 `..` 穿越和符号链接越界。
- 不向前端返回本机绝对路径。
- 项目列表只读元数据；进入项目后才扫描该项目的 `library/`，避免首页遍历数 GB 素材。
- 新增可预览扩展名时，同时检查 MIME 映射、前端预览分支、测试和 Git 审计影响。
- 不删除 SPA fallback。开发服务和 `npm run preview` 的深层路由都必须能直接刷新。
- UI 改动至少检查桌面和约 390 px 手机宽度；不能用“构建通过”代替真实页面和交互检查。
- 继续保持本地优先设计。添加写入、覆盖、移动、删除或批量重命名能力前，必须先获得用户明确授权并设计可恢复方案。

### 5.3 验证矩阵

代码修改完成后，按任务风险执行并报告真实结果：

```bash
npm test
npm run typecheck
npm run build
npm run audit:git-boundary
```

涉及路由、预览或 UI 时，还要启动真实服务验证：

- 首页能列出多个项目；
- 项目封面可读取/替换；
- 深层项目和目录 URL 直接访问、刷新、前进后退均正常；
- Markdown 相对图、图片和视频按任务范围实际预览，音频按任务范围在本地实际试听；
- 非法项目、非法封面和越界路径被拒绝；
- 桌面与移动端无关键布局溢出，控制台无新增错误。

## 6. Git 与交付边界

- 永远不要执行 `git add .` 后直接提交；先看 `git status --short --ignored`，再精确暂存自己修改的程序文件。
- `git ls-files workspace` 只能返回 `workspace/.gitkeep`。
- `.env.local`、`.qa/`、Playwright 输出、构建产物和真实资源都不得提交。
- 超过 5 MB 的文件默认不应进入仓库；若确属程序资产，先向用户说明并取得确认。
- 未经用户明确授权，不提交、不推送、不建分支、不创建 PR/MR、不部署。
- 不为清除无关告警而修改用户已有文件；验证和报告应限定到本次变更。

最终交付必须分层说明：改了什么、资源是否真实复制/生成、做了哪些实际查看或播放、自动化检查结果、Git 边界结果，以及仍缺少的真实验收。若缺少关键实测，就明确标为未验证或受阻。
